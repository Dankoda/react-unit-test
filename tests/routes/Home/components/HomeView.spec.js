describe('(Componnet) Home', () => {
  let _wrapper
  beforeEach(() => {
  })

  it('home test', () => {
    expect(true).to.equal(true)
  })
  it('Renders a welcome page')
  it('Renders an duck image')

})
